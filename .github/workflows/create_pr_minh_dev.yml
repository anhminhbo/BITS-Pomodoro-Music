name: Create pull request to minh-dev for members
run-name: ${{ github.actor }} is triggering this GitHub Action 🚀
on:
  push:
    branches-ignore:
      - "main"
      - "minh-dev"
jobs:
  createPullRequest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout minh-dev
        uses: actions/checkout@v3
        with:
          ref: minh-dev

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          commit-message: Merge to minh-dev
          committer: GitHub <noreply@github.com>
          author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
          signoff: false
          branch: minh/github-action
          base: minh-dev
          delete-branch: false
          token: ${{ secrets.GITHUB_TOKEN }}
          title: "Merge to minh-dev"
          body: |
            Auto PR to minh-dev
            - Auto-generated by [create-pull-request][1]

            [1]: https://github.com/peter-evans/create-pull-request
          labels: |
            Automated PR to minh-dev
          draft: false
